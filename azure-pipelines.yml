trigger: none

pool:
  name: self-hosted-linux

steps:
- checkout: self
  fetchDepth: 0

# 1️⃣ Prepare SonarCloud (CLI MODE)
- task: SonarCloudPrepare@4
  inputs:
    SonarCloud: 'SonarCloud-Service'
    organization: 'vishwa2102'
    scannerMode: 'CLI'
    configMode: 'manual'
    cliProjectKey: 'vishwasureshkumar9950121_aks-zero-touch'
    cliProjectName: 'aks-zero-touch'
    cliSources: '.'
    extraProperties: |
      sonar.language=cs
      sonar.sourceEncoding=UTF-8

# 2️⃣ (Optional) Build – only for validation, not for Sonar
- script: |
    dotnet build dotnet-folder.csproj --configuration Release
  displayName: Build (.NET)

# 3️⃣ Run analysis
- task: SonarCloudAnalyze@4

# 4️⃣ Publish Quality Gate
- task: SonarCloudPublish@4
  inputs:
    pollingTimeoutSec: '300'
